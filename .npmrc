# Hoist better-sqlite3 to root node_modules for native binding resolution
public-hoist-pattern[]=*better-sqlite3*
public-hoist-pattern[]=*sqlite-vec*

# Allow build scripts for native modules
enable-pre-post-scripts=true
